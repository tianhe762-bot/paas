[project]
name = "paas"
version = "0.1.0"
description = "PAAS - Personal Auto Accounting System (QQ/Telegram chat-based accounting)"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
pythonpath = ["."]
filterwarnings = ["ignore::DeprecationWarning"]
